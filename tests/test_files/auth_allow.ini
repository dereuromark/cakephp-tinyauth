; ----------------------------------------------------------
; Everyone can access index and view actions
; ----------------------------------------------------------
Users = index, view
; ----------------------------------------------------------
; Only index action is public for admin prefix
; ----------------------------------------------------------
admin/Users = index
; ----------------------------------------------------------
; All Offers controller actions in Extras plugin are public, except private
; ----------------------------------------------------------
Extras.Offers = "!superPrivate", *
