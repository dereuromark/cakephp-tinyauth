[ControllerName]
* = * ; All roles can access all actions

[Users]
index = user ; Only user role can access index action

[Sales]
index, view = admin ; Only admin role can access index and view actions

[Offers]
* = editor,admin ; Editor and admin roles have access to all actions

; ...